// Userscript entry point - this gets built into the final userscript
// The actual userscript content comes from the existing wordle-debug-production.user.js

export default function() {
    // This is just a placeholder - the actual userscript is generated 
    // from the existing file in the build process
    console.log('Userscript entry point loaded');
}